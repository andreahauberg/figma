---
const { title, textColor, backgroundColor } = Astro.props;
---

<button
  class="small-size"
  style={{ color: textColor, background: backgroundColor }}>{title}</button
>

<style>
  button {
    display: flex;
    width: 10;
    padding: 0.9375rem 2.125rem;
    align-items: flex-start;
    gap: 0.625rem;
    flex-shrink: 0;

    border-radius: 2.625rem;
    background: #181818;

    color: #fff;
    line-height: 1.8;
  }
</style>
